# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.linkdump/

# 🔔 fetch-festival-dates 가 그때그때 만드는 임시 목록 (IndexNow 로 보낼 주소).
# 커밋할 것이 아니다 — 다음 실행이 다시 만든다.
changed-urls.txt
ping-log.txt

# 🎬 릴스 컷·영상 — scripts/make-reel.py 가 만든다. 저장소에 담지 않는다.
#    사진(docs/잠수교-사진)과 대본만 있으면 언제든 다시 만들 수 있고,
#    mp4 는 2MB 라 회차마다 쌓이면 저장소가 무거워진다.
reel-out/

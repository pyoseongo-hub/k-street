# 네이버 클라우드 플랫폼 Maps — Web Dynamic Map Client ID
# console.ncloud.com → AI·Application Service → AI NAVER API → Maps → Application 등록
# 발급받은 Client ID를 .env.local(gitignore됨)에 실제 값으로 넣을 것 — 여기엔 넣지 않는다.
VITE_NAVER_MAPS_CLIENT_ID=

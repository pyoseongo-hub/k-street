# 🧭 코스 추천 — **시간이 아니라 「몇 곳」으로 정한다**

**2026-09-11 사장님 결정.** 만들기 전에 이 문서부터 읽을 것.

---

## ✅ 1단계는 만들었다 — 「가까운 순 보기」 (2026-09-12)

사장님이 직접 그려 주신 그림대로 만들었다:

> "구를 선택 / 아래로 리스트 거리순 나열 / 1km 미만 장소 ㅇㅇ시장 ㅇㅇ공원 …
>  이 장소에서 2km 미만장소 … / 그냥 나열하면 먼데부터 갈수잏자나"
> "거리순 보기 · 테마별보기" — **두 개 다**
> "내 위치는 매일 숙소에서 계획 짜면 같은장소니 구를 선택하게"

**새 화면을 만들지 않았다.** 이미 있는 동네 탭 안에 보기 전환을 붙였다 —
손님이 화면을 더 배울 필요가 없다.

| 무엇 | 어디에 |
|---|---|
| 거리 계산·띠 묶기 | `src/lib/districtDistance.ts` |
| 화면(보기 전환·띠 제목·거리 딱지) | `src/components/DistrictExplorer.tsx` |
| 꾸밈 | `src/index.css` 의 `.view-toggle` · `.band-head` · `.pr-dist` |
| 자료 검사(25개 구 전부) | `scripts/check-district-distance.ts` |
| 화면 검사(진짜 브라우저) | `scripts/check-distance-view.mjs` |

**세 가지를 못박아 뒀다:**

1. **기준점은 그 구의 대표 역** — 그 구의 곳들이 가장 많이 끼고 있는 역이다
   (종로구 → 안국역 3호선, 11곳). 위치 허락을 안 받아도 되고, 숙소에서 내일 계획을
   짜는 손님에게 「내 위치 근처」는 아무 쓸모가 없다.
2. **직선 거리라고 적는다** — 「걸어서 12분」은 우리 자료에 없는 값이다.
   실제로 걸으면 길이 꺾이므로 이보다 멀다.
3. **역 자료가 없는 구에서는 거리순을 아예 감춘다** — 지어내지 않는다.
   (지금은 25개 구 전부 대표 역이 나온다. 못 찾은 구 0.)

⚠️ **줄 제목은 `grid-column: 1 / -1` 이 없으면 조용히 망가진다.**
1080px 이상에서 `.place-list` 가 3칸 격자가 되기 때문에, 띠 제목이 칸 하나를
차지해 **카드 사이에 끼어 버린다.** `tsc` 도 폰 화면도 이걸 못 잡는다 —
`check-distance-view.mjs` 의 ④번이 이것만 보고 있다.

~~다음은 ②「몇 곳 코스」다~~ → **만들었다. 바로 아래.** 설계는 맨 아래 「만드는 방법」 그대로 갔다.

---

## ✅ 2단계도 만들었다 — 「내 주변 코스」 (2026-09-22)

사장님: *"내 주변 코스 받는게 목표야 지금 구조로 만들어"*

| 무엇 | 어디에 |
|---|---|
| 코스 계산(최근접 이웃·겹침 거르기·오늘 여는지) | `src/lib/nearbyCourse.ts` |
| 화면(접힌 단추·고르기·결과) | `src/components/NearbyCourse.tsx` (동네 탭 맨 위) |
| 문구 12개 언어 | `src/lib/translations.ts` 의 `nearbyCourse*` |
| 꾸밈 | `src/index.css` 의 `.nc-*` |
| 자료 검사 | `scripts/check-nearby-course.ts` — **배포마다 자동**(deploy.yml) |
| 화면 검사(진짜 브라우저) | `scripts/check-nearby-view.mjs` |

### 🚨 설계안에서 **뺀 것 둘** — 사장님이 먼저 그은 선이라서

사장님이 보내 주신 설계안에는 이 둘이 있었다:

| 설계안에 있던 것 | 왜 안 넣었나 |
|---|---|
| 사용가능시간 「1시간·2시간·반나절·하루」 | **사장님이 2026-09-11에 직접 뺀 것**이다 — *"그시간을 잴수없어 거기에 머무르는 시간은 개개인이 틀리니"* |
| 결과의 「예상 소요시간 2시간 10분」 | 같은 이유. 머무는 시간이 없으면 계산이 안 된다 |

대신 **위 표의 왼쪽/오른쪽 규칙 그대로** 만들었다 —
손님은 **몇 곳**을 고르고(3·5·7), 화면에는 **미터**만 뜬다.
「이동방식(도보/대중교통)」은 **한 구간의 거리 한도**로 옮겼다(1.2km / 4km).
「도보 15분」이라고 적으려면 걷는 속도를 알아야 하는데 그건 우리 자료에 없다.
한도 숫자 자체도 화면에 안 적는다 — 손님이 보는 것은 **실제 거리**뿐이다.

🔔 **이건 기계가 지킨다.** `check-nearby-view.mjs` ④번이 **12개 언어 화면을 다 열어**
「40분」·「giờ」 같은 말이 있는지 본다. 일부러 넣어 울려 봤고 **영어·베트남어 둘 다 잡았다.**
나중에 누가 친절하게 「약 40분」을 넣어도 `tsc` 는 통과하니, 사람이 아니라 기계가 막아야 한다.

### 🧭 「구 선택」을 밀어내지 않는다

사장님이 2026-09-12에 하신 말 — *"내 위치는 매일 숙소에서 계획 짜면 같은장소니 구를 선택하게"* —
은 **지금도 맞다.** 그래서 1단계(가까운 순 보기)를 그대로 뒀다. 둘은 쓰는 때가 다르다:

| | 언제 | 기준점 |
|---|---|---|
| 가까운 순 보기 (1단계) | 숙소에서 **내일**을 계획할 때 | 그 구의 대표 역 |
| 내 주변 코스 (2단계) | **이미 밖에 나와** 있을 때 | 손님의 GPS |

그래서 「내 주변 코스」는 동네 탭 맨 위에 **접힌 단추 하나**로만 있는다.
자리를 안 먹고, 펴도 위치를 안 묻는다 — **「코스 만들기」를 눌러야** 묻는다.

### 🐞 만들면서 검사로 잡은 사고 둘 (둘 다 화면은 멀쩡했다)

**① 「5곳 코스」가 한 군데였다.** 중구를 돌려 보니 **모두 100m** 로 나왔다:

```
남대문 종합상가 · 숭례문 수입상가 · 남대문시장 · 남대문 액세사리상가 · 남대문 중앙상가
```

다섯 중 **셋이 좌표가 글자 하나까지 같았다.** 전부 남대문시장 한 덩어리다.
서초구도 김장대축제와 우리술대축제가 같은 자리(양재 시민의숲)였다.
숫자는 하나도 안 틀렸는데 **손님이 받는 말이 거짓**이다 —
「5곳을 도는 코스」라 해 놓고 실제로는 한 군데에 서 있게 된다.
→ 이미 넣은 곳에서 **150m 안에 붙은 후보는 건너뛴다**(`MIN_GAP`).

⚠️ **남는 한계** — 좌표가 똑같은 것들 중 **어느 이름이 뽑힐지는 정해지지 않는다.**
「남대문시장」이 아니라 「남대문 종합상가」가 뽑힐 수 있다. 어느 쪽이 대표인지를
우리 자료가 모르기 때문이다. 고치려면 **곳 자료에 대표/부속 관계를 적어야 한다** —
그건 자료 일이지 계산 일이 아니다. 지어내지 않고 그냥 둔다.

**② 오늘 안 하는 축제를 「여기서 600m」라고 적고 있었다.** 영등포구 코스에
「서울세계불꽃축제」와 「한강 종이비행기 축제」가 시장·공원과 나란히 들어왔다.
**손님은 그걸 보고 걸어간다. 가면 아무것도 없다.**
Kfood 에 이미 한 줄로 적혀 있는 것과 같다 —
**「없는 메뉴를 적는 게 값이 틀린 것보다 나쁘다 — 손님이 그걸 시키려다 헛걸음한다.」**
→ 코스에는 **오늘 열려 있는 축제만** 넣는다. 날짜를 모르는 축제는 **뺀다.**

📌 **목록 화면과 잣대가 다른 것이 맞다.** 동네 탭·계절 탭은 「무엇이 있나」를 보는
자리라 축제를 날짜와 함께 보여 줘도 된다. 코스는 **「지금 여기서 걸어가라」**는 말이다.

### 📊 지금 나오는 코스 (2026-09-22 실측)

`npm run check-nearby-course` — 구마다 대표 역에 서 있다고 치고 3곳 코스를 만들어 본다.

| | 🚶 걸어서 3곳 | 🚇 지하철도 3곳 |
|---|---|---|
| 서울 25개 구 | **19개 구** | 25개 구 전부 |
| 부산 16개 구·군 | 10개 | 16개 전부 |

3곳을 못 채우는 구에서는 **있는 만큼만 주고 그렇다고 말한다**(`nearbyCourseShort`).
조용히 적게 주지 않는다.

🚨 **부산은 `nearest-station.json` 에 자료가 한 건도 없다** (364건이 전부 서울이다).
   그래서 **1단계 「가까운 순 보기」는 부산에서 아예 안 뜬다** — 기준점이 역이라서다.
   **2단계는 부산에서도 돈다** — 기준점이 손님의 GPS 라 역 자료가 필요 없다.
   (부산에도 역 자료를 채우려면 Actions → **Nearest station**.)

---


---

## 🚨 시간으로 하지 않는다 — 사장님이 못박은 것

> "코스추천은 그시간을 잴수없어 거기에 머무르는 시간은 개개인이 틀리니
> 그러니 숫자로 몇개 코스를 넣을지 정하게 하는게 좋을거 같아"

**맞는 말이고, 이 앱의 정확도 원칙과도 그대로 맞는다.**

「반나절 코스」·「2시간 코스」라고 적으려면 **각 곳에 얼마나 머무는지**를 알아야 하는데,
그건 사람마다 다르다. 박물관에서 20분 보는 사람과 두 시간 보는 사람이 있다.
우리 자료에 **소요시간이 없다** — 없는 걸 적으면 그게 곧 틀린 정보다.

| ❌ 적으면 안 되는 것 | ✅ 적어도 되는 것 |
|---|---|
| 「2시간 코스」 | 「3곳 코스」 |
| 「반나절이면 충분」 | 「걸어서 모두 1.8km」 |
| 「관람 40분」 | 「다음 곳까지 600m」 |
| 「아침에 가기 좋다」 | 「가장 가까운 역: 안국역 3호선」 |

오른쪽 칸은 **전부 좌표로 계산한 사실**이다. 지어낸 값이 하나도 없다.

---

## 손님이 고르는 것

```
어느 구      종로구 · 마포구 · …          (25개 구)
몇 곳        3곳 · 5곳 · 7곳
무엇을       시장 · 골목 · 박물관 · 산책 · 축제  (골라도 되고 안 골라도 되고)
```

**「몇 곳」이 핵심이다.** 시간을 묻지 않으니 거짓말할 일이 없고,
손님은 자기 속도를 안다 — "3곳이면 오전에 되겠네"는 손님이 판단한다.

---

## 우리가 이미 가진 재료

| 재료 | 어디에 | 몇 곳 |
|---|---|---|
| 좌표(위도·경도) | `src/data/coords.json`, 곳 자료 | 304 / 309 |
| 가장 가까운 역·거리 | `src/data/nearest-station.json` | 283 |
| 구 · 갈래 | 곳 자료 | 전부 |
| 사진 | `photo-gallery` 등 | 285 |

**좌표가 있으면 두 곳 사이 거리는 정확히 계산된다**(하버사인). 그게 코스의 뼈대다.

---

## 만드는 방법 (초안)

1. 손님이 고른 **구 + 갈래**로 후보를 추린다
2. 시작점을 잡는다 — **가장 가까운 역이 있는 곳**부터가 자연스럽다
3. 거기서 **가장 가까운 다음 곳**을 차례로 잇는다(최근접 이웃)
4. 고른 **곳 수**만큼 채우면 멈춘다
5. 화면에는 **거리만** 적는다: 「① → ② 600m · ② → ③ 1.1km · 모두 1.8km」

⚠️ **최근접 이웃은 최단 경로가 아니다.** 그래도 괜찮다 — 우리가 약속하는 건
   「가까운 것끼리 묶었다」이지 「가장 짧은 길」이 아니다. **말한 것만 지키면 된다.**

---

## 🚫 관광공사 「여행코스」는 못 쓴다 — 확인했다

2026-09-11에 실제로 불러 봤다:

```
서울 여행코스(25) 받는 중...
   ↳ 관광공사가 알려 준 전체 건수: 0
  → 0건
```

**관광공사가 직접 0이라고 답했다.** 우리 호출이 틀린 게 아니다.
그러니 코스는 **우리 좌표로 직접 만들어야 한다.**

📌 오히려 잘된 일일 수 있다 — 남들이 다 쓰는 같은 코스가 아니라 **우리만의 코스**가 된다.

---

## 순서

사장님이 정한 순서는 **① 깃허브 요금 → ② 거리 채우기 → ③ 코스**다.
②가 끝나야 코스에 넣을 골목이 넉넉해진다.

⚠️ 지금 **9개 구에 골목이 0곳**이다(중랑·성북·강북·도봉·노원·금천·동작·관악·서초).
   그 구에서 「골목 3곳 코스」를 고르면 **빈 화면이 나온다.**
   → 코스를 내기 전에 **곳이 모자란 구·갈래 조합은 아예 못 고르게** 막을 것.
      케이푸드가 음식별 페이지에서 쓴 것과 같은 규칙이다(「얇은 페이지는 안 만든다」).

// 🚧 **서울 자료로만 만든 기능** — 다른 도시에서는 숨긴다.
//
// 사장님 결정 (2026-09-17, 부산을 열면서): *"부산에서는 안 보이게."*
//
// ── 왜 숨기나 ─────────────────────────────────────────────────────────────
//   아래 세 가지는 **서울시·서울메트로가 내놓은 자료**로 만든 것이라, 부산에는
//   애초에 같은 자료가 없다:
//     · 🍁 단풍길·꽃길 107곳 — 서울시 「서울 단풍길」 자료
//     · ☔ 비 오는 날 지하상가 — 서울시설공단이 운영하는 25곳
//     · 🧳 짐 보관(T-Luggage) — **서울메트로** 역에만 있다
//
//   부산을 고른 손님에게 이 칸을 그대로 보여 주면 **서울 것을 부산 것인 양** 내놓게 된다.
//   빈 목록을 보여 주는 것도 답이 아니다 — 「부산은 아직 안 채웠나 보다」가 아니라
//   「이 앱은 고장났나」로 읽힌다.
//   그래서 **아예 안 그린다.** 없는 것을 있는 척하지 않는 쪽이 이 앱의 원칙에 맞다.
//
// ⏳ 부산 자료를 구하면 그때 이 가림막을 걷는다. 걷을 자리가 한 곳이라 찾기 쉽다.
import { useCity } from "./useCity";

/** 지금 보는 도시가 서울인가 — 위 세 기능은 참일 때만 그린다. */
export function useIsSeoul(): boolean {
  return useCity().cityKey === "seoul";
}

# K-Street — 작업 전에 반드시 읽을 것

서울 동네마다 열리는 축제·시장·꽃길·산책로·둘레길·박물관을 외국인 관광객에게 보여주는 안내 서비스.
가제(working title) — 정식 이름은 아님, 상표·앱스토어 중복 확인 전.

**Kfood(dongne-hanip)와는 완전히 별개 프로젝트다.** 같은 제작자가 만들지만 코드·배포·브랜드를 공유하지 않는다.
다만 정확도 원칙은 그대로 물려받는다 — 아래 참고.

## 💚 이 앱은 평생 무료다 — 가입도 결제도 없다 (사용자 결정, 2026-09-04)

> "이 앱은 무료고 평생 무료로 사용할 수 있어. 가입이나 결제는 절대 없어."

**이건 '지금은 무료'가 아니라 '영원히 무료'다.** 아래 세 가지는 어떤 이유로도 넣지 않는다:

| 넣지 않는 것 | 왜 |
|---|---|
| 🚫 **회원가입 · 로그인** | 며칠 쓰는 관광객에게 계정을 만들라고 하는 순간 대부분 그냥 나간다. 이메일도 안 받는다 |
| 🚫 **결제 · 구독 · 유료 기능** | "이 기능은 유료" 칸을 하나라도 만들면 무료라는 말이 거짓이 된다 |
| 🚫 **무료 체험 기간 · 나중에 유료 전환** | 가장 나쁜 쪽이다. 믿고 쓰던 사람을 배신한다 |

※ **손님에게 안 받는다는 뜻이지, 수익이 아예 없다는 뜻은 아니다** — 아래 「그럼 뭘로 먹고사나」 참고.

**설계에 미치는 영향 — 이게 실제로 코드를 바꾼다:**

- 손님별 자료는 **그 폰 안에만** 둔다(localStorage). 서버에 계정별로 쌓아 두면 로그인이
  필요해지고, 그러면 위 원칙이 무너진다. 저장한 곳(`src/lib/savedPlaces.ts`)이 그렇게 돼 있고,
  앞으로 만드는 것(방문 기록·메모·일정 등)도 전부 같은 방식이어야 한다.
- **다른 기기와 안 맞춰지는 것은 버그가 아니라 선택이다.** "기기 간 동기화를 넣자"는 제안은
  곧 "로그인을 넣자"는 말이다 — 그때 이 줄로 돌아온다.
- **서버를 두지 않는다.** GitHub Pages 정적 배포를 유지한다. 서버가 생기면 고정비가 생기고,
  고정비가 생기면 언젠가 그걸 메우자는 얘기가 나온다. 지금 고정비는 **0원**이다.

### 그럼 뭘로 먹고사나 — 손님에게 안 받고 **광고 쪽**으로 (사용자 결정, 2026-09-04)

> "사용자 모집되서 내 앱을 광고하거나, 거기에 발전하면 배너 정도만 할 거야."

무료라는 건 **손님이 돈을 안 낸다**는 뜻이지 이 앱이 아무 쓸모가 없다는 뜻이 아니다.
허용되는 것은 둘, 그것도 **이 순서**다:

| 순서 | 무엇 | 조건 |
|---|---|---|
| ① | **자기 앱(Kfood) 알리기** | 사람이 어느 정도 모인 뒤. 우리 것이라 남의 돈이 안 끼어든다 — 가장 깨끗한 방법이고 이게 원래 구상이었다 |
| ② | **배너 광고** | "거기에 발전하면" — ①로 자리를 잡은 **뒤**에나 생각한다. **배너 정도까지**다 |

🚨 **넘지 않는 선 — 광고와 '노출 팔기'는 다르다.**
배너는 화면 한 칸을 빌려 주는 것이고, 노출 팔기는 **목록 순서를 돈으로 바꾸는 것**이다.
Kfood 초심 1번("식당에게 돈을 받고 노출을 올려주지 않는다")이 여기에도 그대로 걸린다 —
K-Street의 축제·시장·골목 목록도 마찬가지다. 돈 낸 곳을 위로 올리거나, 광고를 카드처럼
생긴 모양으로 목록에 섞지 않는다. **광고는 광고로 보여야 한다.**

⚠️ 배너를 실제로 넣을 때 걸리는 것 셋 (그때 다시 볼 것):
- **개인정보 안내가 필요해진다.** 구글 애드센스 같은 곳은 쿠키를 쓰고, 우리 손님은 유럽·
  동남아에서 온 외국인이라 GDPR 동의 창이 붙는다 — 지금은 쿠키가 하나도 없어서 그 창이 없다.
- **첫 화면이 느려진다.** 광고 스크립트는 바깥에서 받아 온다. 오늘 구글 글꼴을 화면 밖으로
  빼서 12.8초를 0.26초로 줄여 놨는데(§출시 전 검수 ④), 광고를 잘못 넣으면 그게 도로 돌아온다.
- **로그인은 여전히 없다.** 배너는 계정을 요구하지 않는다 — 이 점은 위 원칙과 안 부딪힌다.

📌 **①이 먼저다.** 배너부터 넣자는 제안이 나오면 이 줄을 꺼낼 것 — 사람이 모이기 전의
배너는 돈도 안 되고 앱만 싸구려로 보이게 한다.

---

## 출시 범위 — 서울만 (사용자 결정, 2026-08-24)

> "일단 서울부터 출시하고 무료배포. 호응이 좋으면 전국 단위 진행. 서울 외 지역은 일단 안 보이게."

- **무료배포.** → 2026-09-04에 이것이 **평생 무료·가입 없음·결제 없음**으로 확정됐다(맨 위 항목 참고).
  유료화·구독은 "지금 안 한다"가 아니라 **안 한다.**
- **서울 외 지역은 코드 차원에서 숨긴다.** `src/config/launchScope.ts`의 `LAUNCH_REGIONS`가 게이트다 —
  `seed.ts`의 `ALL_PLACES`는 이 게이트를 통과한 것만 내보낸다. 나중에 다른 도시 데이터를 추가해도
  `LAUNCH_REGIONS`를 넓히기 전까지는 화면에 안 뜬다. **절대로 이 필터를 우회해서 개별 컴포넌트에서
  "일단 보여주기"로 처리하지 말 것** — 게이트가 한 곳에만 있어야 나중에 전국으로 열 때 한 줄로 끝난다.
- **전국 확장 조건은 아직 숫자로 정해지지 않았다.** "호응이 좋으면"이라고만 했다 — 구체적인 기준(설치 수,
  DAU 등)은 사용자가 나중에 정할 일이지 지금 임의로 만들지 않는다.

## 배포·수익화 단계 (사용자 구상, 2026-08-24)

> K-Street에서 사용자 확보 → Kfood 홍보 → (앱 출시) → 설치 수 → Kfood 수익화

이 순서 자체(먼저 사용자를 모으고, 그다음에 Kfood를 연결하고, 수익화는 맨 마지막)는 Kfood의 "수익화보다
커버리지" 원칙과 같은 방향이라 맞다. 다만 두 가지는 짚어 둘 필요가 있다 — 다음에 이 얘기가 다시 나오면
참고할 것:

1. **"앱 출시"가 수익화의 전제조건은 아니다.** 지금 K-Street은 이미 PWA라서 앱스토어 없이도
   설치(홈 화면 추가)·재방문이 가능하다. 다만 이 PWA 코드가 버려지는 건 아니다 —
   나중에 진짜 네이티브 앱스토어 등록이 필요해지면 Capacitor 같은 도구로 지금 이 웹앱을
   그대로 감싸서 iOS/Android 패키지로 낼 수 있다. 그러니 "지금 웹으로 가느냐 vs 나중에 앱스토어로
   가느냐"는 양자택일이 아니라 순서의 문제다.
2. **Kfood는 "식당에게 돈 받고 노출 안 판다"는 원칙이 이미 있다** (Kfood CLAUDE.md 1번).
   그래서 Kfood 쪽 수익화는 광고·제휴 커미션·유료 기능 같은 다른 모델이어야 한다 —
   이건 아직 정해지지 않았다. K-Street에서 사용자를 모으는 것과는 별개로, Kfood 쪽 수익 모델
   자체를 다음에 한 번 짚고 넘어가는 게 좋다.

## 지금 상태 (2026-08-25 갱신)

React + TypeScript + Vite로 스캐폴딩한 뼈대 단계. `npm run dev`로 바로 확인 가능.

- **디자인 — 다크 엘레강스 테마가 기본값이다(2026-08-25, 사용자가 참고 화면 공유).**
  `src/styles/tokens.css` 참고 — 시스템 다크모드 대응이 아니라 브랜드 정체성 자체로
  다크 톤을 :root 기본값으로 넣었다(라이트 모드 없음). 세리프 헤드라인 + 민트 틸
  스몰캡스 라벨 + 앰버 강조색 조합. 사진은 아직 못 넣었다 — 포토코리아(공공누리 1유형,
  https://phoko.visitkorea.or.kr) 등 실제 사진 사이트가 이 세션(샌드박스)에서는 네트워크
  차단이라 직접 못 받아온다. 대신 `src/components/SeasonArt.tsx`(계절별 벡터 일러스트,
  `src/lib/useRotatingSeed.ts`로 시간마다 모티프·팔레트가 바뀜)로 대체해 뒀다.
- **실제 사진 — TourAPI(한국관광공사) 파이프라인 추가함(2026-08-25).** 사용자가 공공데이터포털에서
  TourAPI 인증키를 발급받았다. `scripts/fetch-tour-images.mjs`를 `TOUR_API_KEY=키 node scripts/fetch-tour-images.mjs`로
  실행하면 축제 이름으로 검색해서 공공누리 1유형 사진을 `src/data/tour-images.json`에 저장한다
  (이 리포는 서버가 없는 정적 사이트라 키를 클라이언트 코드에 못 넣는다 — 그래서 로컬 스크립트로
  미리 받아 URL만 저장해 둔다). 이 파일이 채워지면 `MonthlyFestivalPanel`이 SeasonArt 대신
  실제 사진을 자동으로 쓴다(`사진: 한국관광공사` 크레딧 칩 포함, 공공누리 1유형 출처 표시 의무).
  이름이 확실히 일치하는 것만 저장하므로(정확도 원칙) 매칭 안 된 축제는 계속 일러스트로 남는다.
  ⚠️ apis.data.go.kr는 이 세션(샌드박스)에서도 접속이 된다(더미 키로 테스트해 HTTP 403 — 네트워크
  차단이 아니라 인증 실패였다) — 다음 세션은 사용자에게 물어서 여기서 바로 돌려도 되고, 사용자가
  로컬에서 돌려도 된다.

- `src/data/seed.ts` — 25개 구 × 6개 카테고리 시드 데이터. **전부 웹 검색 요약 기반**이라
  등록 전 공식 페이지로 재확인이 필요하다. `confirmed: false`인 항목은 값이 아니라
  "아직 못 찾은 빈 칸"이다 — UI에서도 그렇게 표시해야 한다("확인 필요", 흐리게).
  박물관·미술관은 9/25 → 20/25로 보강함(강북·구로·동작·성동·중랑 5곳만 남음).
- `scripts/audit-seed.mjs` — Kfood의 audit-seed.mjs와 같은 목적. `npm run audit-seed`로 실행.
  id 충돌·자리표시자 모순 등은 ❌(blocking)로 막고, 카테고리별 커버리지·날짜 누락 축제는
  ⚠️(warning)로만 보여준다. **seed.ts를 고칠 때마다 돌릴 것.**
- **PWA(홈 화면에 추가)** — `vite-plugin-pwa`로 설정. 앱스토어 없이 아이폰 사파리·안드로이드
  크롬 모두에서 "홈 화면에 추가"가 된다. 아이콘은 `public/icons/`(직접 생성, 저작권 문제 없음).
  배포 전략 자체가 "웹앱 하나로 아이폰까지 커버"다 — 자세한 배경은 아래 "배포·수익화 단계" 참고.
- `src/components/MonthlyFestivalPanel.tsx` — 홈 상단, 축제 전용 시간형 UI(계절·월 선택 → 그 달 축제).
- `src/components/DistrictExplorer.tsx` — 홈 하단, 나머지 5개 카테고리(시장·꽃길·산책로·둘레길·박물관) 전용
  장소형 UI. **네이버 지도 SDK 연동 완료(2026-08-25)** — `src/components/SeoulMap.tsx`가 서울 전체를
  실제 지도로 보여준다(`src/lib/naverMaps.ts`, Client ID는 `.env.local`의 `VITE_NAVER_MAPS_CLIENT_ID`,
  `.env.example` 참고). **다만 구별·장소별 마커는 아직 없다** — 구 좌표를 공식 출처(서울 열린데이터광장 등)로
  검증하기 전까지는 지어내지 않는다(정확도 원칙). 그래서 실제 탐색은 여전히 아래 25개 구 그리드로 한다.
  이 세션(샌드박스)은 아웃바운드 프록시가 네이버 지도 CDN을 막아 직접 스크린샷 확인은 못 했다 —
  실제 사용자 브라우저에서는 제한이 없으니 로컬(`npm run dev`)에서 확인할 것.
  ⚠️ **좌표 확보 경로가 정해졌다(2026-08-25).** `scripts/fetch-tour-images.mjs`가 이제 사진과 함께
  TourAPI가 등록해 둔 실제 mapx/mapy도 `tour-images.json`에 저장한다(관광공사 공식 좌표라 지어낸 게
  아니다). TourAPI 키가 나오면 이 좌표로 지도 위에 진짜 핀을 찍는 작업으로 이어갈 것 — "UX 아이디어"
  절의 번호 핀 지도 구상 참고.
- `src/components/WeatherStrip.tsx`, `src/lib/weather.ts` — 실시간 서울 날씨(기온·체감온도·시각).
  Open-Meteo(키 불필요, CORS 열림)로 클라이언트에서 바로 호출한다. 참고 화면에 있던 "인기 명소가
  그늘로 이동 중" 같은 코멘트 문구는 지어낸 것이라 안 넣었다 — 실제 관측값만 보여준다.
- **TourAPI(한국관광공사) 키 — 2026-08-25 신청 완료, 승인 대기 중.** api.visitkorea.or.kr(한국관광
  콘텐츠랩)에서 OpenAPI로 신청함. 키 나오면 `scripts/fetch-tour-images.mjs` 실행할 것.
- `src/config/launchScope.ts`, `src/data/districts.ts` — "서울만 노출" 게이트(위 출시 범위 참고).
- `.github/workflows/deploy.yml` — main에 푸시하면 GitHub Pages로 자동 배포(무료).
  **딱 한 번, 리포 Settings → Pages → Source를 "GitHub Actions"로 바꿔야 켜진다** — API로 못 하는
  설정이라 사용자가 직접 눌러야 한다. 켜지면 `https://pyoseongo-hub.github.io/k-street/`에서 보인다.
  배포 전에 `npm run audit-seed`를 돌려서 ❌ blocking이 있으면 배포 자체를 막는다.

## UX 아이디어 — 결정 대기, 아직 실행 안 함 (사용자 메모, 2026-08-25)

> "참고용 맵을 보여주고 그때그때의 행사를 표시. 외국인이 구나 동네의 개념이 없으니
>  직관적으로 이 정보 저장." (먹방로드 앱 스크린샷 첨부 — 구/동 텍스트 목록이 아니라
>  지도 위에 핀으로 바로 표시하는 방식)

**아직 결정된 것 아니다 — 저장만 해 두는 것.** 다음에 이 방향으로 갈지 말지 사용자가
다시 판단할 것. 그때까지 구현 착수하지 말 것(좌표 조사 포함).

- 지금 구조(위 "지금 상태" 55~60번 줄)는 구 이름 텍스트 그리드가 탐색 수단이다 —
  외국인 관광객은 "종로구가 어디쯤인지" 감이 없으니 이게 안 와닿을 수 있다는 지적.
  먹방로드처럼 지도 위에 바로 핀을 찍고, 그 위에 "지금 시즌에 뭐가 열리는지"를
  겹쳐서 보여주는 방식이 더 직관적일 수 있다는 아이디어.
- 이 방향으로 가려면 장소별(최소 축제별) 실제 좌표가 있어야 한다 — 지금 `seed.ts`엔
  좌표가 하나도 없다. 좌표를 웹 검색 요약으로 채우려 해봤는데, 검색 요약 자체가
  "정확한 좌표를 못 찾았다, 대략 이 근처다"처럼 불확실한 답을 주는 경우가 많았다
  (이 세션은 Wikipedia 등 원출처 페이지도 접근이 막혀 있어 이중 확인이 더 어려웠다).
  즉 좌표를 채우려면 검색 요약보다 확실한 방법(공식 좌표 API, 지도에서 직접 클릭해
  좌표 읽기 등)이 필요하다 — **정확도 원칙(추측 좌표 금지)과 바로 부딪히는 지점.**
- 구현하게 되면: ① Place에 `lat`/`lng` 필드 추가 → ② 축제부터(장소가 비교적
  뚜렷한 곳 위주로) 좌표 확보 → ③ SeoulMap에 마커 렌더링 추가 → ④ 구 그리드는
  좌표 없는 항목의 대체 수단으로 남겨 둠(정확도 원칙: 빈 칸이 틀린 정보보다 낫다).

## 🗓️ 작업 주기 — 하는 날엔 하나를 끝까지 (사용자 결정, 2026-09-01)

사용자 질문: "새로 추가 화면은 그 일주일 모아서 하는 게 좋은가 그날그날 하는 게 좋은가."

**화면(기능) 작업은 그날그날. 다만 매일 하라는 뜻이 아니라, 하는 날에는 한 가지를 끝까지 한다.**
일주일에 하루만 잡아도 되지만 **반쯤 만들어 두고 며칠 지나는 게 가장 나쁘다.**

근거는 2026-09-01 하루에 실제로 겪은 것이다. 사고 셋이 전부 5분 안에 잡혔는데,
**전부 그날 만든 것**이라 어디를 건드렸는지 기억하고 있었기 때문이다:

| 증상 | 진짜 원인 | 왜 빨리 잡았나 |
|---|---|---|
| 이름이 회색 상자로 보임 | 주석을 규칙 한가운데 끼워 넣어 선택자가 `.pr-compact .pr-name-link`로 붙음 | 그날 오전에 내가 넣은 주석 |
| 워크플로가 초록인데 저장 안 됨 | `node … \| tee` 는 pipefail이 없으면 node가 죽어도 성공 | 워크플로를 방금 씀 |
| 구글 403 | 열쇠의 'API 제한사항'에 Cloud Translation API가 빠짐 | 오류 문구를 즉시 고쳐 원인을 좁힘 |

며칠 뒤였으면 "왜 이러지"부터 시작해야 했다. 그리고 **사용자가 캡처로 짚어 주는 게
가장 빠른 검증**인데, 그러려면 화면이 올라가 있어야 한다.

**반대로 자료 채우기는 모아서/자동으로 한다** — 이미 그렇게 걸어 뒀다:
축제 날짜는 매달 1일 자동 · 번역은 새로 생긴 것만 · 사진은 하루 3곳.

## 다음에 할 일 (순서대로)

1. ~~지도 SDK 연동~~ — **완료(2026-08-25)**. 네이버지도 Web Dynamic Map, Client ID 발급 완료.
   `src/components/SeoulMap.tsx`가 서울 전체 실제 지도를 보여준다. 길찾기는 직접 계산하지 않고
   네이버·카카오 앱으로 넘긴다(구글맵은 한국에서 정확한 길찾기를 못 준다 — 정부의 정밀지도 반출
   규제 때문). **남은 일**: 구별(또는 장소별) 좌표를 공식 출처(서울 열린데이터광장 등)로 확인해서
   지도 위에 실제 마커를 찍는 것 — 지금은 검증 안 된 좌표라 마커가 없다.
2. **박물관·미술관 카테고리 마무리** — 20/25 확인됨. 강북·구로·동작·성동·중랑 5개 구만 남음(`seed.ts` 참고).
3. **서대문·양천 외 23개 구 동별(법정동) 소규모 축제 전수조사** — 이번엔 두 구만 확인했다.
4. **공식 데이터 API 연동** — 서울시 문화행사 정보(OA-15486), 전통시장 현황(OA-1176), TourAPI 등.
   `seed.ts`를 이 API들로 교체/보강하는 방향. 상세 소스 목록은 기획 문서 05번 섹션 참고.
   ⚠️ 이 API들 중 좌표(위경도)가 포함된 것을 찾으면 위 1번의 "남은 일"도 같이 풀린다.
   📌 **전국 확장 시**: 네이버 축제 정보 (https://m.search.naver.com/search.naver?query=전국축제정보)
5. ~~다국어~~ — **완료(2026-09-01)**. 구글 번역으로 11개 언어 × 524개 문구를 채웠다
   (`src/data/place-translations.json`, Actions "Translate places"). 원문 5,411자라
   무료 한도 월 50만 자의 12%밖에 안 쓴다 — 곳이 늘어도 여유가 크다.
   ⚠️ **이름·설명·기간만 번역하고 주소는 한국어 그대로 둔다.** 주소는 뜻을 아는 게 아니라
   택시 기사에게 보여주는 것이다. 같은 이유로 화면에는 **번역 이름 아래 한국어 원문**을
   함께 띄운다(`src/lib/placeText.ts`).
   ⚠️ 이름 안의 구·동은 `notranslate`로 못박는다 — 구글은 한자 지명을 뜻으로 푼다
   (Kfood: 영화동 → 「映画館」). 그래도 일본어 「広場市場」(실제 廣藏市場)처럼 새는 게 있다.
   눈에 띄면 그 항목만 손으로 고친다.

6. ~~축제 17곳 좌표~~ — **자동으로 바꿨다(2026-09-02, 사용자 지시 "자동").**
   원인은 스크립트가 **축제 '이름'으로** 지도를 검색한다는 것이었다. 축제는 며칠만
   열리는 행사라 지도에 등록된 장소가 아니어서, 이름으로는 영원히 0건이 난다.
   → 축제마다 **실제로 열리는 곳**을 적은 표 `src/data/festival-venues.json`을 만들고
   (강남페스티벌 → 코엑스, 한성백제문화제 → 올림픽공원 …, 19곳 전부 구청·서울문화포털·
   관광공사로 근거 확인), `fetch-coords.mjs`가 이름 대신 그 장소를 검색한다.
   **Fetch coordinates는 이제 매주 월요일 새벽에 저절로 돈다** — 사람이 누를 일이 없다.
   · 구 일치 검사와 네이버 교차확인은 그대로라 엉뚱한 좌표는 여전히 안 들어간다.
   · 새 축제를 넣었는데 좌표가 안 붙으면 **표에 한 줄만** 추가한다 — 좌표를 손으로
     적지 않는다(추측 좌표 금지 원칙).
   · 남은 곳은 `npm run festival-todo`가 '장소표' 칸에 🤖 자동 / **사람이 확인**으로
     갈라서 보여 준다(`docs/축제-빈칸.md`).

## 📷 사진 — 곳당 7장 (2026-09-02)

사용자가 관광콘텐츠랩(api.visitkorea.or.kr/#/hubTourSearch)을 열어 캡처해 짚었다:
"사진 이쁜걸로 교체 / 전수조사 / 지금것보다 여기가 퀄리티좋다".

우리는 목록 API가 주는 **대표 이미지 한 장**(firstimage)만 쓰고 있었는데,
`detailImage2`로 받아 보니 **246곳에 1,720장**이 있었다(평균 7장, 96%가 두 장 이상).
북한산 자락길 29장 · 서울 중부시장 28장 · 강서역사문화거리 24장.

```bash
# Actions → Fetch tour gallery. 전체로 돌리려면 limit에 all 이라고 적는다.
```

**대표 사진을 다른 걸로 바꾸지는 않았다.** 축제는 대표 이미지가 포스터인 경우가
많아 아쉽지만, 어느 게 더 이쁜지 기계가 고르면 멀쩡한 사진을 버리는 쪽이 더
잦다(좌표에서 아파트·성당이 걸리던 그 문제). 첫 장은 그대로 두고 **고르는 건
손님에게 맡긴다** — 카드를 누르면 넘어간다(`PlacePhoto.tsx`).

🚨 **워크플로가 조용히 실패하는 세 가지** — 오늘 셋 다 겪었다. 새 워크플로를
만들 때 반드시 확인할 것:

| 증상 | 원인 | 고치는 법 |
|---|---|---|
| 초록불인데 아무 일도 안 함 | `node ... \| tee` — 파이프 끝이 성공하면 앞이 죽어도 초록 | `set -o pipefail` + `2>&1` |
| 전체로 돌렸는데 일부만 처리 | 입력을 **빈 칸으로 주면 default로 되돌아간다** | 전체는 `all` 같은 **명시적 값**으로 |
| "저장했다"는데 파일이 없음 | `git diff`는 **git이 모르는 새 파일을 못 본다** | `git add` 먼저, `git diff --cached`로 검사 |

⚠️ 구글 번역은 고유명사를 뜻으로 푼다 — 하늘공원이 일본어로 「空公園」이 됐다
(공식은 ハヌル公園). 화면에 **번역 이름 아래 한국어 원문**을 함께 띄우므로 손님이
가게에서 말할 수는 있다. 눈에 띄는 것만 손으로 고친다.

## 📸 사진 게이트와 하루 3곳 채우기 (사용자 결정, 2026-09-01)

> "사진 없는장소 일단 가리기 앱에 안보이게 / 하루에 수동 몇개씩 구청 서치 무료 이미지있는거
> 사용 / 루틴으로 하루 세개씩 시작 / 이미지 채운 것은 보임으로"

**사진이 없는 곳은 앱에 안 나온다.** `seed.ts`의 `hasPhoto` 게이트다.
지우는 게 아니라 가리는 것이라, `src/data/manual-photos.json`에 사진 한 줄을 넣으면
그 곳은 바로 다시 나타난다.

지금 443곳 중 **264곳만 보이고 179곳이 가려져 있다.**

| 칸 | 전체 | 보임 |
|---|---|---|
| 🍢 골목·거리 | 39 | **39** |
| 🎪 축제 | 88 | 57 |
| 🛒 시장 | 85 | 64 |
| 🏛️ 박물관 | 99 | 63 |
| 🚶 산책길 | 47 | 20 |
| ⛰️ 등산로 | 53 | 19 |
| 🌸 **꽃길** | 32 | **2** ⚠️ |

⚠️ **꽃길 탭이 비어 보이는 건 자료가 없어서가 아니다.** 사람이 조사한 25개 구 꽃길 30곳에
사진이 없어서 가려진 것뿐이다. 그래서 채우는 순서를 **꽃길부터**로 못박아 뒀다
(`scripts/list-photo-todo.mjs`의 `PRIORITY`). 하루 3곳이면 열흘이면 되살아난다.

```bash
npm run photo-todo          # 오늘 채울 3곳 + 구청 검색 링크
npm run photo-todo -- --all # 남은 곳 전부(진행 상황 확인)
```

🤖 **루틴이 매일 오전 9시(KST)에 돈다** — `trig_019W6jJvtYMS2XbXzMwS2edp`
("K-Street 사진 채우기 — 하루 3곳"). 새 세션을 띄워 위 스크립트를 돌리고 3곳을 알려 준다.
그 세션에는 MCP 도구가 없을 수 있어 **Bash와 git만으로** 되게 프롬프트를 짜 뒀다.

🚨 **저작권 — 여기서 타협하지 않는다.**
- 공공기관(구청·서울시)이 **공공누리로 공개한 사진만** 쓴다.
- 블로그·인스타·핀터레스트·구글 이미지 검색 결과는 **전부 남의 저작물이라 안 된다.**
- `source`(출처)·`license`(이용 조건)를 **못 적는 사진은 넣지 않는다.**
  `manualPhotos.ts`가 그 두 칸이 없는 항목을 아예 무시하도록 코드로 막아 뒀다 —
  "확인 못 한 것은 넣지 않는다"를 사진에도 그대로 적용한 것이다.

## 🗣️ 사용자에게 설명하는 방식 (사용자 지시, 2026-09-01)

> "나는 초보자라 자세하고 간단한 설명이 필요해. 대화식으로 한단계씩."
> "이렇게 나열하지 말고 하나씩 이미지포함 설명."

**이 프로젝트의 사용자는 개발자가 아니다.** 명령어를 한꺼번에 나열하면 어디서 막혔는지
본인도 모르고, 혼자 여러 번 다시 하다가 며칠이 지나간다(2026-09-01에 실제로 그랬다 —
관광공사 API가 "안 된다"고 며칠을 보냈는데 원인은 키가 아니라 셸 문법이었다).

지켜야 할 것:

- **한 번에 한 단계만 준다.** 5단계를 한꺼번에 쓰지 않는다. 한 단계를 끝내고
  결과를 확인한 뒤 다음을 준다. 사용자가 "다음"이라고 할 때까지 기다린다.
- **화면을 그림으로 보여 준다.** 아티팩트(HTML)로 **터미널 모형**을 그려서
  "지금 화면이 이렇게 생겼고, 성공하면 이렇게 바뀐다"를 눈으로 보여 준다.
  아티팩트는 **같은 파일 경로로 다시 게시**해서 한 URL을 단계마다 갱신한다
  (링크가 여러 개면 사용자가 어느 게 최신인지 모른다).
- **성공 신호와 실패 신호를 따로 명시한다.** "아무 메시지도 안 나오는 게 정상"처럼
  초보자가 불안해하는 지점을 미리 짚는다. 노란 `npm warn`이 오류가 아니라는 것도.
- **오류 대응표를 미리 만들어 둔다.** 나올 수 있는 오류를 표로 정리해서 같이 준다.
  막혔을 때 사용자가 스스로 어디를 볼지 알 수 있어야 한다.
- **명령어는 복사용 한 줄로.** 손으로 치게 하지 않는다(대문자·역슬래시 하나로 실패한다).
- **막히면 멈추고 물어보라고 반복해서 말한다.** 혼자 반복하는 게 가장 큰 시간 낭비였다.
- **비유를 쓴다.** `add`/`commit`/`push` = 물건 담기/송장 쓰기/부치기 같은 식으로.

⚠️ **Windows 환경이다.** Mac/리눅스 문법을 그대로 주면 안 된다:

| 하려는 것 | ❌ 안 되는 것 (bash) | ✅ Windows |
|---|---|---|
| 폴더 이동 | `cd /c/Users/...` | `cd C:\Users\...` |
| 파일 목록 | `ls -la` | `dir` |
| 환경변수 + 실행 | `KEY=x node a.mjs` | `set KEY=x` 후 줄 바꿔 `node a.mjs` |

🚨 **`%`가 들어간 값은 PowerShell + 작은따옴표로 넣는다.** 공공데이터포털 인증키는
`%2B`·`%2F`·`%3D` 를 포함하는데, **cmd에서 `%`는 변수 기호**라 값이 깨진다.
반드시 이렇게 안내할 것 — 며칠 막혔던 진짜 원인이 이거였다:

```powershell
$env:TOUR_API_KEY = '여기에_Encoding_키'   # 작은따옴표 필수
$env:TOUR_API_KEY.Length                    # 실행 전에 항상 확인시킬 것
node scripts/fetch-tour-places.mjs
```

🪪 **공공데이터포털 인증키는 Encoding 쪽을 쓴다.** 화면에 크게 보이는 값은 Decoding
(원본, `+`·`/`·`==`)이라 마우스로 긁으면 틀린 키가 복사된다. **"인증키 복사(Encoding)"
버튼을 누르라고** 명시할 것. 스크립트가 `serviceKey`를 그대로 URL에 붙이기 때문이다.

## 정확도 원칙 (Kfood에서 그대로 가져옴)

- **확인 못 한 것은 넣지 않는다.** 빈 칸이 틀린 정보보다 낫다. `seed.ts`의 `confirmed: false`가 이 원칙의 코드 표현이다.
- **요금·개장 시기처럼 해마다 바뀌는 값은 적지 않고 공식 링크로 안내한다.** 예: 간송미술관은
  상시 개방이 아니라 연 2회(5·10월)만 특별전시 형태로 연다 — `seed.ts`의 `note`에 이미 적어 뒀다.
  이런 예외를 UI에서 뭉개면(모든 박물관을 "언제든 갈 수 있는 곳"으로 다루면) 틀린 정보가 된다.
- **검색 요약 하나는 근거가 아니다.** 같은 곳을 다른 검색어로 두 번 이상 확인한 값만 `confirmed: true`로 둔다.
- **동네(법정동)는 도로명으로 추측하지 말고 지번을 본다** — 자치구 단위로도 이미 여러 구를 가로지르는
  하천이 있다(양재천, 중랑천 등). 향후 데이터 모델에 "하천"처럼 구 경계를 넘는 상위 개체를 추가하는 걸 검토할 것
  (기획 문서 03-7 참고).

## 참고 문서

전체 기획(콘텐츠 카테고리, 25개 구 조사 결과, 벤치마킹, 홈 화면 설계 결정 등)은
claude.ai 아티팩트 "K-Street"에 있다 — 이 리포에는 옮겨 적지 않았다. 링크는 이 프로젝트를
시작한 대화 세션에 있으니, 다음 세션은 사용자에게 링크를 요청할 것.
